const role = ['user', 'super_admin', 'admin'];

export const userConstant = {
  role,
};

export const userFilterableFields = ['searchTerm', 'email', 'role'];

export const userSearchableFields = ['email', 'name', 'phoneNumber'];
